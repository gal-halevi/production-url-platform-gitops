name: smoke-dev

on:
  push:
    branches: [main]
    paths:
      - "envs/dev/values.yaml"
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  smoke:
    uses: ./.github/workflows/_smoke-env.yml
    with:
      env: dev
    secrets:
      URL_PLATFORM_INGRESS_ENDPOINT: ${{ secrets.URL_PLATFORM_INGRESS_ENDPOINT }}