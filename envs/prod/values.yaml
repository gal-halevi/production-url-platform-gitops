images:
  urlService:
    tag: sha-1fd02b9
  redirectService:
    tag: sha-01e0766
  analyticsService:
    tag: sha-01e0766
replicaCount:
  urlService: 3
  redirectService: 3
  analyticsService: 2
  postgres: 1
ingress:
  # we will handle ssl in a following PR
  # annotations:
  #   nginx.ingress.kubernetes.io/ssl-redirect: "true"
  hosts:
    api: api.url-platform.example
    redirect: r.url-platform.example
    analytics: analytics.url-platform.example
urlService:
  baseUrl: http://r.url-platform.example
global:
  appEnv: prod
